# Connect Four — Vite + React (каркас)

Каркас застосунку гри "Чотири в ряд (Connect Four)" без бізнес-логіки та без роутингу.

## Старт

```bash
npm install
npm run dev
```

Сторінки перемикаються через локальний state в `App.jsx`.
